{
  "name": "rollup-plugin-rollup",
  "version": "0.1.0",
  "description": "Inline rollup bundles for Web Workers and such.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "scripts": {
    "build": "rollup -c",
    "pretest": "npm run build",
    "test": "rollup -c test/rollup.config.js && open-cli http://localhost:5000 && sirv test --single",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meh/rollup-plugin-svg-store.git"
  },
  "keywords": [
    "rollup",
    "rollup-plugin"
  ],
  "files": [
    "dist"
  ],
  "author": "meh.",
  "license": "WTFPL",
  "bugs": {
    "url": "https://github.com/meh/rollup-plugin-rollup/issues"
  },
  "homepage": "https://github.com/meh/rollup-plugin-rollup",
  "devDependencies": {
    "open-cli": "^6.0.1",
    "rollup": "^2.29.0",
    "sirv-cli": "^1.0.6"
  },
  "dependencies": {
  }
}
